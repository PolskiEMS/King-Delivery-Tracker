"use client";

import { useEffect, useState } from "react";
import { Bell, CheckCircle2, MonitorCog, Save, Shield, UserCog } from "lucide-react";
import { Sidebar } from "@/components/dashboard/sidebar";

type StoredUser = { id?: string; email?: string; firstName?: string; lastName?: string; role?: string };
type Preferences = { routeAlerts: boolean; deliveryAlerts: boolean; orderAlerts: boolean; highPriorityOnly: boolean; compactLists: boolean; autoRefresh: boolean; defaultView: "dashboard" | "routes" | "deliveries"; language: "pl" | "en" };
const preferencesKey = "kingDeliveryDispatcherSettings";
const userKey = "kingDeliveryCurrentUser";
const defaults: Preferences = { routeAlerts: true, deliveryAlerts: true, orderAlerts: true, highPriorityOnly: false, compactLists: false, autoRefresh: true, defaultView: "dashboard", language: "pl" };

function Toggle({ label, description, checked, onChange }: { label: string; description: string; checked: boolean; onChange: (checked: boolean) => void }) {
  return <button type="button" onClick={() => onChange(!checked)} className="flex w-full items-center justify-between gap-4 rounded-2xl border border-white/10 bg-[#020813]/60 p-4 text-left transition hover:border-amber-400/30"><span><span className="block font-semibold text-white">{label}</span><span className="mt-1 block text-sm text-slate-400">{description}</span></span><span className={`relative h-7 w-12 rounded-full border transition ${checked ? "border-amber-300 bg-amber-400" : "border-white/10 bg-white/[0.06]"}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white transition ${checked ? "left-6" : "left-1"}`} /></span></button>;
}

export default function SettingsPage() {
  const [user, setUser] = useState<StoredUser>({});
  const [preferences, setPreferences] = useState<Preferences>(defaults);
  const [message, setMessage] = useState<string | null>(null);

  useEffect(() => {
    try { setUser(JSON.parse(window.localStorage.getItem(userKey) ?? "{}") as StoredUser); } catch { window.localStorage.removeItem(userKey); }
    try { setPreferences({ ...defaults, ...(JSON.parse(window.localStorage.getItem(preferencesKey) ?? "{}") as Partial<Preferences>) }); } catch { window.localStorage.removeItem(preferencesKey); }
  }, []);

  function updatePreference<Key extends keyof Preferences>(key: Key, value: Preferences[Key]) { setPreferences((current) => ({ ...current, [key]: value })); }
  function saveSettings() { window.localStorage.setItem(preferencesKey, JSON.stringify(preferences)); window.localStorage.setItem(userKey, JSON.stringify(user)); window.dispatchEvent(new Event("storage")); setMessage("Ustawienia zostały zapisane lokalnie dla tego panelu."); }
  function resetSettings() { setPreferences(defaults); setMessage("Przywrócono ustawienia domyślne. Kliknij Zapisz, aby utrwalić zmianę."); }

  return <main className="min-h-screen bg-[#020813] text-slate-100"><div className="flex min-h-screen"><Sidebar /><section className="flex-1 bg-[radial-gradient(circle_at_top_right,rgba(251,191,36,0.14),transparent_34%),linear-gradient(180deg,#061123_0%,#020813_44%,#020813_100%)]"><header className="flex min-h-20 flex-col gap-4 border-b border-white/10 bg-[#020813]/90 px-4 py-5 shadow-2xl shadow-black/20 backdrop-blur sm:px-6 md:flex-row md:items-center md:justify-between lg:px-9"><div><h1 className="text-2xl font-bold text-white">Ustawienia</h1><p className="mt-1 text-sm text-slate-400">Konfiguracja profilu dyspozytora, powiadomień i widoku aplikacji.</p></div><div className="flex flex-wrap gap-3"><button onClick={resetSettings} className="rounded-xl border border-white/10 bg-white/[0.04] px-4 py-2 text-sm font-bold text-slate-200">Przywróć domyślne</button><button onClick={saveSettings} className="inline-flex items-center gap-2 rounded-xl bg-amber-400 px-4 py-2 text-sm font-bold text-[#020813]"><Save className="h-4 w-4" />Zapisz</button></div></header><div className="mx-auto grid w-full max-w-[1600px] gap-6 px-4 py-5 sm:px-6 lg:grid-cols-[1.1fr_0.9fr] lg:p-8"><div className="space-y-6"><article className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 shadow-2xl shadow-black/20"><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-xl border border-amber-400/20 bg-amber-400/10 text-amber-300"><UserCog className="h-5 w-5" /></span><div><h2 className="text-lg font-bold text-white">Profil dyspozytora</h2><p className="text-sm text-slate-400">Dane widoczne w panelu bocznym i nagłówkach.</p></div></div><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="text-sm font-semibold text-slate-300">Imię<input value={user.firstName ?? ""} onChange={(event) => setUser((current) => ({ ...current, firstName: event.target.value }))} className="mt-2 w-full rounded-xl border border-white/10 bg-[#020813]/70 px-3 py-2 text-slate-100 outline-none focus:border-amber-400/50" /></label><label className="text-sm font-semibold text-slate-300">Nazwisko<input value={user.lastName ?? ""} onChange={(event) => setUser((current) => ({ ...current, lastName: event.target.value }))} className="mt-2 w-full rounded-xl border border-white/10 bg-[#020813]/70 px-3 py-2 text-slate-100 outline-none focus:border-amber-400/50" /></label><label className="text-sm font-semibold text-slate-300 md:col-span-2">E-mail<input value={user.email ?? ""} onChange={(event) => setUser((current) => ({ ...current, email: event.target.value }))} className="mt-2 w-full rounded-xl border border-white/10 bg-[#020813]/70 px-3 py-2 text-slate-100 outline-none focus:border-amber-400/50" /></label></div></article><article className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 shadow-2xl shadow-black/20"><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-xl border border-amber-400/20 bg-amber-400/10 text-amber-300"><Bell className="h-5 w-5" /></span><div><h2 className="text-lg font-bold text-white">Powiadomienia</h2><p className="text-sm text-slate-400">Wybierz typy zdarzeń pokazywane w centrum powiadomień.</p></div></div><div className="mt-5 space-y-3"><Toggle label="Alerty tras" description="Nowe i zmienione trasy, przypisanie kierowcy oraz status realizacji." checked={preferences.routeAlerts} onChange={(value) => updatePreference("routeAlerts", value)} /><Toggle label="Alerty dostaw" description="Problemy, dostawy w toku i zakończone pozycje na trasach." checked={preferences.deliveryAlerts} onChange={(value) => updatePreference("deliveryAlerts", value)} /><Toggle label="Alerty zamówień" description="Nowe zamówienia i zamówienia oczekujące na przypisanie." checked={preferences.orderAlerts} onChange={(value) => updatePreference("orderAlerts", value)} /><Toggle label="Tylko wysoki priorytet" description="Domyślnie eksponuj tylko pilne komunikaty operacyjne." checked={preferences.highPriorityOnly} onChange={(value) => updatePreference("highPriorityOnly", value)} /></div></article></div><aside className="space-y-6"><article className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 shadow-2xl shadow-black/20"><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-xl border border-amber-400/20 bg-amber-400/10 text-amber-300"><MonitorCog className="h-5 w-5" /></span><div><h2 className="text-lg font-bold text-white">Widok aplikacji</h2><p className="text-sm text-slate-400">Preferencje interfejsu zapisane w przeglądarce.</p></div></div><div className="mt-5 space-y-4"><label className="block text-sm font-semibold text-slate-300">Domyślny ekran<select value={preferences.defaultView} onChange={(event) => updatePreference("defaultView", event.target.value as Preferences["defaultView"])} className="mt-2 w-full rounded-xl border border-white/10 bg-[#020813] px-3 py-2 text-slate-100"><option value="dashboard">Pulpit</option><option value="routes">Trasy</option><option value="deliveries">Dostawy</option></select></label><label className="block text-sm font-semibold text-slate-300">Język<select value={preferences.language} onChange={(event) => updatePreference("language", event.target.value as Preferences["language"])} className="mt-2 w-full rounded-xl border border-white/10 bg-[#020813] px-3 py-2 text-slate-100"><option value="pl">Polski</option><option value="en">English</option></select></label><Toggle label="Kompaktowe listy" description="Zmniejsz odstępy w tabelach i listach operacyjnych." checked={preferences.compactLists} onChange={(value) => updatePreference("compactLists", value)} /><Toggle label="Automatyczne odświeżanie" description="Przygotuj panel do cyklicznego pobierania nowych danych." checked={preferences.autoRefresh} onChange={(value) => updatePreference("autoRefresh", value)} /></div></article><article className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 shadow-2xl shadow-black/20"><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-xl border border-emerald-400/20 bg-emerald-400/10 text-emerald-300"><Shield className="h-5 w-5" /></span><div><h2 className="text-lg font-bold text-white">Bezpieczeństwo</h2><p className="text-sm text-slate-400">Sesja lokalna i status konta.</p></div></div><div className="mt-5 rounded-2xl border border-emerald-400/20 bg-emerald-400/10 p-4 text-sm text-emerald-100"><CheckCircle2 className="mb-2 h-5 w-5" />Panel działa w trybie dyspozytora. Zmiany profilu i preferencji są zapisywane lokalnie w przeglądarce.</div>{message ? <p className="mt-4 rounded-xl border border-amber-400/20 bg-amber-400/10 px-4 py-3 text-sm text-amber-100">{message}</p> : null}</article></aside></div></section></div></main>;
}
